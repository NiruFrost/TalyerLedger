# TalyerLedger Master Build Specification

The repository's canonical, complete specification is
[MASTER_BUILD_PROMPT_V3.md](../MASTER_BUILD_PROMPT_V3.md). This stable
documentation path intentionally points to that single source so two copies do
not drift.

Phase-specific validation records may narrow work to the current phase, but
they do not override the master specification.
